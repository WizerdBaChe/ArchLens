# ==============================================================================
# ArchLens v1.0 - Requirements Inventory
# ==============================================================================
# 遵循 MVP 篩選原則 (DD-001)，本專案目前 100% 採用 Python 內建標準函式庫：
# [os, json, fnmatch, zipfile, pathlib, argparse, typing]
#
# 現階段處於「零外部套件依賴 (Zero-dependency)」狀態。
# 此檔案目前留空，以便未來擴充 Web (如 fastapi, uvicorn) 或 GUI 時精準追蹤。
# ==============================================================================

fastapi
uvicorn
python-multipart